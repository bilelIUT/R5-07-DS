name: Q1 - Archive TOTO

on:
  push:
    paths:
      - 'TOTO.txt'
      - '.github/workflows/Q1.yaml'

jobs:
  archive:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copie TOTO.txt Q1_archive_TOTO.txt
        run: cp TOTO.txt Q1_archive_TOTO.txt

      - name: Commit and push archive file
        run: |
          git config --global user.name "bilelIUT"
          git config --global user.email "bilelchriki2811@gmail.com"
          git add Q1_archive_TOTO.txt
          git commit -m "Ajout de Q1_archive_TOTO.txt"
          git push
